class Transform {
    constructor(rotation = Quaternion.identity(), position = new Vector(0, 0, 0)) {
        this.rotation = rotation;
        this.position = position;
    }
}

export { Transform }
